import {TicketStatusColorPipe} from './ticket-status-color.pipe';

describe('TicketStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new TicketStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
