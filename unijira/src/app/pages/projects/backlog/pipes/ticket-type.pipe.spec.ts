import {TicketTypePipe} from './ticket-type.pipe';

describe('TicketTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TicketTypePipe();
    expect(pipe).toBeTruthy();
  });
});
