import {ReleaseStatusPipe} from './release-status.pipe';

describe('ReleaseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
