import {ReleaseStatusColorPipe} from './release-status-color.pipe';

describe('ReleaseStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
